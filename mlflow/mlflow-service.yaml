apiVersion: v1
kind: Service
metadata:
  name: mlflow-service
  namespace: ml-platform
  labels:
    app: mlflow
spec:
  ports:
    - port: 5000
      targetPort: 5000
      name: http
  selector:
    app: mlflow
  type: ClusterIP
